---
import Navbar from "../components/Navbar.astro";
import Layout from "../layouts/Layout.astro";
---

<Layout title="Registro | TechZone Gaming" description="Crea tu cuenta para comprar en TechZone Gaming.">
  <Navbar currentPath={Astro.url.pathname} />
  <main class="mx-auto w-full max-w-xl px-4 py-12">
    <h1 class="mb-6 text-3xl font-bold text-slate-900">Crear cuenta</h1>
    <form id="register-form" class="space-y-4 rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
      <div>
        <label for="nickname" class="mb-1 block text-sm text-slate-700">Nickname</label>
        <input id="nickname" name="nickname" required class="w-full rounded-md border border-slate-300 bg-white px-3 py-2 text-slate-800 outline-none focus:border-fuchsia-500" />
      </div>
      <div>
        <label for="email" class="mb-1 block text-sm text-slate-700">Correo</label>
        <input id="email" name="email" type="email" required class="w-full rounded-md border border-slate-300 bg-white px-3 py-2 text-slate-800 outline-none focus:border-fuchsia-500" />
      </div>
      <div>
        <label for="password" class="mb-1 block text-sm text-slate-700">Contrasena</label>
        <input id="password" name="password" type="password" required class="w-full rounded-md border border-slate-300 bg-white px-3 py-2 text-slate-800 outline-none focus:border-fuchsia-500" />
      </div>
      <button class="w-full rounded-md bg-fuchsia-600 px-4 py-2 font-semibold text-white hover:bg-fuchsia-500">Registrarse</button>
      <p id="message" class="text-sm"></p>
    </form>
  </main>
</Layout>

<script>
  import { api } from "../lib/api";

  const form = document.querySelector<HTMLFormElement>("#register-form");
  const message = document.querySelector<HTMLParagraphElement>("#message");

  form?.addEventListener("submit", async (event) => {
    event.preventDefault();
    if (!form) return;
    const data = new FormData(form);
    const nickname = String(data.get("nickname") ?? "");
    const email = String(data.get("email") ?? "");
    const password = String(data.get("password") ?? "");

    try {
      await api.auth.registro({ nickname, email, password });
      if (message) {
        message.textContent = "Usuario registrado correctamente. Ahora puedes iniciar sesion.";
        message.className = "text-sm text-emerald-600";
      }
      form.reset();
    } catch (error) {
      if (message) {
        message.textContent = error instanceof Error ? error.message : "Error al registrar usuario.";
        message.className = "text-sm text-rose-600";
      }
    }
  });
</script>
